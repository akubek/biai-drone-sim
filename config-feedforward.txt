[NEAT]
fitness_criterion     = max
fitness_threshold     = 100000
pop_size              = 150
reset_on_extinction   = True
no_fitness_termination = True

[DefaultGenome]
# Wielkość sieci
num_inputs              = 20
num_hidden              = 0
num_outputs             = 2

# Opcje aktywacji węzłów
activation_default      = sigmoid
activation_mutate_rate  = 0.0
activation_options      = sigmoid

# Opcje agregacji węzłów
aggregation_default     = sum
aggregation_mutate_rate = 0.0
aggregation_options     = sum

# Opcje biasu węzłów (TEGO BRAKOWAŁO)
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_max_value          = 30.0
bias_min_value          = -30.0
bias_mutate_rate        = 0.7
bias_mutate_power       = 0.5
bias_replace_rate       = 0.1

# Opcje response węzłów (TEGO TEŻ MOGŁO ZARAZ ZABRAKNĄĆ)
response_init_mean      = 1.0
response_init_stdev     = 0.0
response_max_value      = 30.0
response_min_value      = -30.0
response_mutate_rate    = 0.0
response_mutate_power   = 0.0
response_replace_rate   = 0.0

# Opcje mutacji strukturalnych (dodawanie/usuwanie neuronów i połączeń)
conn_add_prob           = 0.6
conn_delete_prob        = 0.1
node_add_prob           = 0.3
node_delete_prob        = 0.1

# Opcje wag połączeń (jak mocno sygnał przechodzi przez sieć)
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_max_value        = 30
weight_min_value        = -30
weight_mutate_rate      = 0.8
weight_mutate_power     = 0.8
weight_replace_rate     = 0.1

# Geny połączeń (czy mutacja może odciąć jakiś sensor)
enabled_default         = True
enabled_mutate_rate     = 0.01

feed_forward            = True
initial_connection      = full

# Opcje kompatybilności genomów
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient   = 0.5

[DefaultSpeciesSet]
compatibility_threshold = 2.5

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 15
species_elitism      = 2

[DefaultReproduction]
elitism            = 3
survival_threshold = 0.2
